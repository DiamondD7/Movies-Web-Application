@model IEnumerable<MovieModel>

<h1 class="text-center">Fitness</h1>
<div>
    <input type="search" placeholder="Search" style="width:90%" class="search-bar-fitness" />
    <button type="submit" class="btn-search">
        <svg xmlns="http://www.w3.org/2000/svg" class="search-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
    </button>
</div>

<div class="Movie-List">
    @foreach (var i in Model)
    {
        var imageSource = "/images/Fitness/" + i.MovieName + ".jpg";
        <img src="@imageSource" style="width:260px" />
    }
</div>